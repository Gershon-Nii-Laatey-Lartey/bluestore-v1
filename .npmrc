# BlueStore Ghana - Package Management Configuration
# Ensure consistent package management across all environments

# Use exact versions for better reproducibility
save-exact=true

# Use npm as the package manager
package-manager=npm

# Set registry to npm
registry=https://registry.npmjs.org/

# Enable audit for security
audit=true

# Fund information
fund=false

# Engine strict mode
engine-strict=true 